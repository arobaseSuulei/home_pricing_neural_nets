## Training a home pricing prediction model using neural nets with keras

# state of the art

A neural nets is using on deep learning for prediction. We’re going here to predict the price of a house using the model. First of all, is more a pedagogical approach to learn more about some neural nets concepts as normalization, loss and metrics fonction, adam optimizer, keras approach and more

